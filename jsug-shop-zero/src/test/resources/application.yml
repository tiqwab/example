spring:
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
    session:
        store-type: hash_map

logging:
    level:
        org:
            hibernate:
                sql: DEBUG
                type: TRACE
